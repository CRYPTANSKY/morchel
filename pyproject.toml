[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "morchel"
version = "0.1.0"
description = "Read-only agent handoffs for inspectable onchain evidence"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [{name = "CRYPTANSKY"}]

[tool.setuptools]
packages = ["morchel"]

